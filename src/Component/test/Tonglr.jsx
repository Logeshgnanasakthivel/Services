import React from 'react'

export const Tonglr = ({fun}) => {
    // const handle=()=>{
    //    onClick(prev => (prev === 'black' ? 'white' : 'black'))
    // }
    
  return (
    <div onClick={fun}>
        hello
    </div>
  )
}
